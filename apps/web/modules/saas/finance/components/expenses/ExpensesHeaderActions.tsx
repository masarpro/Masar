"use client";

// Header actions are now integrated into ExpensesList component
// This component is kept for backward compatibility but renders nothing
export function ExpensesHeaderActions({ organizationSlug }: { organizationSlug: string }) {
	return null;
}
