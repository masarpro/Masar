# برومبت تحسين قسم التنفيذ — PLAN MODE

> **الوضع:** PLAN MODE — لا تكتب كود. فقط حلل الوضع الحالي وقدم خطة تفصيلية كاملة للتنفيذ.

---

## السياق العام

أنت تعمل على منصة **مسار** — نظام إدارة مقاولات SaaS للمقاولين الصغار والمتوسطين في السعودية. المنصة مبنية على:

- **Next.js 16** + **React 19** + **TypeScript**
- **Prisma 7** + **PostgreSQL** (Supabase)
- **ORPC** للـ API (type-safe RPC)
- **Better Auth** للمصادقة
- **Tailwind CSS** + **shadcn/ui** للواجهة
- **i18n** عربي/إنجليزي مع RTL

### فلسفة مسار

**"بساطة جذرية"** — واجهة بسيطة للمقاول العادي + عمق اختياري للمدير/المهندس. لا نبني ERP ثقيل. نبني أداة تُباع بسرعة وتُستخدم فعلاً.

---

## الوضع الحالي لقسم التنفيذ (ما هو موجود)

### قاعدة البيانات الحالية

```prisma
model ProjectMilestone {
  id              String           @id @default(cuid())
  projectId       String
  name            String
  description     String?
  startDate       DateTime?
  endDate         DateTime?
  actualStartDate DateTime?
  actualEndDate   DateTime?
  progress        Float            @default(0)  // ⚠️ يجب تحويله لـ Decimal
  status          MilestoneStatus  @default(PLANNED)
  sortOrder       Int              @default(0)
  assigneeId      String?
  createdAt       DateTime         @default(now())
  updatedAt       DateTime         @updatedAt
  
  project         Project          @relation(...)
  assignee        User?            @relation(...)
}

enum MilestoneStatus {
  PLANNED
  IN_PROGRESS
  COMPLETED
  DELAYED
  CANCELLED
}
```

### الـ API الحالي (9 endpoints)

| Endpoint | النوع | الوصف |
|----------|-------|-------|
| `projectTimeline.listMilestones` | query | قائمة المراحل |
| `projectTimeline.createMilestone` | mutation | إنشاء مرحلة |
| `projectTimeline.updateMilestone` | mutation | تحديث مرحلة |
| `projectTimeline.deleteMilestone` | mutation | حذف مرحلة |
| `projectTimeline.reorderMilestones` | mutation | إعادة ترتيب |
| `projectTimeline.markActual` | mutation | تسجيل تاريخ فعلي |
| `projectTimeline.startMilestone` | mutation | بدء المرحلة |
| `projectTimeline.completeMilestone` | mutation | إكمال المرحلة |
| `projectTimeline.getHealth` | query | صحة الجدول الزمني |

### الوحدة الميدانية الحالية (projectField — 11 endpoints)

| Endpoint | الوصف |
|----------|-------|
| `projectField.createDailyReport` | تقرير يومي (عمالة، طقس، أعمال منجزة) |
| `projectField.listDailyReports` | قائمة التقارير |
| `projectField.createPhoto` | إضافة صورة |
| `projectField.listPhotos` | قائمة الصور |
| `projectField.deletePhoto` | حذف صورة |
| `projectField.createIssue` | إنشاء مشكلة (عنوان، خطورة) |
| `projectField.listIssues` | قائمة المشاكل |
| `projectField.updateIssue` | تحديث مشكلة |
| `projectField.addProgressUpdate` | إضافة تحديث تقدم |
| `projectField.listProgressUpdates` | قائمة التحديثات |
| `projectField.getTimeline` | جدول زمني ميداني |

### مكونات Gantt الحالية (12 ملف في projects-timeline)

موجود عرض Gantt مبسط لكنه **عرض فقط** — لا يوجد:
- تفاعل Drag & Drop
- علاقات Dependencies
- مسار حرج
- خط أساس Baseline
- WBS هرمي

### الصفحات الحالية

```
/projects/[projectId]/execution          → مراحل + صور + مشاكل
/projects/[projectId]/execution/new-report → تقرير يومي
/projects/[projectId]/execution/new-issue  → إنشاء ملاحظة
/projects/[projectId]/execution/upload     → رفع صور
/projects/[projectId]/timeline            → جانت مبسط (عرض فقط)
/projects/[projectId]/field               → تقارير ميدانية
```

### Enums ذات صلة موجودة

```prisma
enum MilestoneStatus { PLANNED, IN_PROGRESS, COMPLETED, DELAYED, CANCELLED }
enum WeatherCondition { SUNNY, CLOUDY, RAINY, WINDY, DUSTY, HOT, COLD }
enum PhotoCategory { PROGRESS, ISSUE, EQUIPMENT, MATERIAL, SAFETY, OTHER }
enum IssueSeverity { LOW, MEDIUM, HIGH, CRITICAL }
enum IssueStatus { OPEN, IN_PROGRESS, RESOLVED, CLOSED }
```

---

## الفجوات الجوهرية (ماذا ينقص)

### 1. لا يوجد WBS هرمي حقيقي
المرحلة (Milestone) هي المستوى الوحيد. لا توجد أنشطة (Activities) تحت المراحل.

### 2. لا توجد علاقات Dependencies
لا يوجد Finish-to-Start أو أي نوع من الربط بين المراحل/الأنشطة.

### 3. لا يوجد خط أساس Baseline
لا يمكن اعتماد خطة ومقارنة المخطط بالفعلي بشكل رسمي.

### 4. لا يوجد تنبيهات ذكية
لا توجد تنبيهات تأخير أو تحذيرات مبنية على البيانات.

### 5. الانفصال بين التنفيذ والميدان والمالية
الصفحات منفصلة تماماً — لا يوجد ربط بين النشاط والتقرير اليومي والمستخلص.

### 6. جانت غير تفاعلي
الجانت الحالي للعرض فقط — لا يمكن السحب أو التعديل مباشرة.

---

## المطلوب: إعادة تصميم قسم التنفيذ بوضعين

### الفلسفة: طبقتان عرض (Two-Layer UI)

**الوضع الأساسي (Simple Mode)** — الافتراضي لكل مستخدم جديد:
- بطاقات مراحل بسيطة مع شريط تقدم
- تحديث النسبة يدوياً أو من Checklist
- تنبيهات تأخير بسيطة
- صور وتقارير يومية مرتبطة بالمرحلة
- **لا جانت، لا dependencies، لا WBS عميق**

**الوضع المتقدم (Advanced Mode)** — يُفعّل بزر:
- WBS هرمي (مرحلة → أنشطة)
- جانت تفاعلي مع Drag & Drop
- Dependencies (FS, SS, FF, SF + Lag/Lead)
- خط أساس Baseline مع مقارنة Planned vs Actual
- مسار حرج Critical Path
- Lookahead (2-6 أسابيع)
- تقويم عمل مخصص

---

## خطة التنفيذ المطلوبة (4 مراحل)

### Phase 1: ترقية البنية التحتية + الوضع الأساسي المحسّن
> **الهدف:** تحويل المراحل الحالية إلى نظام قابل للتوسع + تحسين UX الأساسي

#### Phase 1A: تحديث Schema (قاعدة البيانات)

**المطلوب — حلل وصمم:**

1. **نموذج Activity جديد** تحت ProjectMilestone:
```
ProjectActivity {
  id, milestoneId, projectId
  name, description
  plannedStart, plannedEnd
  actualStart, actualEnd
  progress (Decimal)
  status (ActivityStatus)
  sortOrder
  assigneeId
  wbsCode (مثل: "1.1", "1.2", "2.1")
  duration (أيام عمل)
  isCritical (Boolean)
  notes
  createdAt, updatedAt
}
```

2. **نموذج ActivityDependency** للعلاقات:
```
ActivityDependency {
  id
  predecessorType (MILESTONE | ACTIVITY)
  predecessorId
  successorType (MILESTONE | ACTIVITY)
  successorId
  dependencyType (FS | SS | FF | SF)
  lagDays (Int, يقبل سالب للـ Lead)
  projectId
}
```

3. **نموذج ProjectBaseline** لخطوط الأساس:
```
ProjectBaseline {
  id, projectId
  name (مثل: "Baseline v1", "بعد أمر تغيير #3")
  snapshotDate
  snapshotData (Json — يحفظ نسخة كاملة من المراحل والأنشطة)
  isActive (Boolean)
  createdBy
  createdAt
}
```

4. **نموذج ProjectCalendar** لتقويم العمل:
```
ProjectCalendar {
  id, projectId
  name
  workDays (Json — مثل [1,2,3,4,5,6] للسبت-خميس)
  holidays (Json — قائمة تواريخ)
  defaultHoursPerDay (Decimal)
  isDefault (Boolean)
}
```

5. **نموذج ActivityChecklist** لقوائم الفحص:
```
ActivityChecklist {
  id, activityId
  title
  isCompleted (Boolean)
  completedAt
  completedBy
  sortOrder
}
```

6. **تحديث ProjectMilestone الحالي:**
   - إضافة `weight` (Decimal) — وزن المرحلة من إجمالي المشروع
   - إضافة `calendarId` (اختياري)
   - إضافة `baselineStartDate`, `baselineEndDate`
   - تحويل `progress` من Float إلى Decimal(5,2)
   - إضافة `progressMethod` (MANUAL | CHECKLIST | ACTIVITIES)
   - إضافة `color` (String?) — لون المرحلة في الجانت

7. **Enums جديدة:**
```prisma
enum ActivityStatus {
  NOT_STARTED
  IN_PROGRESS
  COMPLETED
  DELAYED
  ON_HOLD
  CANCELLED
}

enum DependencyType {
  FINISH_TO_START    // FS — الأكثر شيوعاً
  START_TO_START     // SS
  FINISH_TO_FINISH   // FF
  START_TO_FINISH    // SF — نادر
}

enum ProgressMethod {
  MANUAL
  CHECKLIST
  ACTIVITIES
  QUANTITY   // مستقبلي: من حصر الكميات
}

enum DependencyNodeType {
  MILESTONE
  ACTIVITY
}
```

#### Phase 1B: تحديث API

**endpoints جديدة مطلوبة:**

```
// === الأنشطة ===
projectExecution.listActivities(milestoneId)
projectExecution.createActivity(milestoneId, data)
projectExecution.updateActivity(activityId, data)
projectExecution.deleteActivity(activityId)
projectExecution.reorderActivities(milestoneId, orderedIds)
projectExecution.updateActivityProgress(activityId, progress)
projectExecution.bulkUpdateProgress(updates[])

// === العلاقات (Dependencies) ===
projectExecution.listDependencies(projectId)
projectExecution.createDependency(data)
projectExecution.deleteDependency(dependencyId)
projectExecution.validateDependencies(projectId) // كشف حلقات ومشاكل

// === خط الأساس ===
projectExecution.createBaseline(projectId, name)
projectExecution.listBaselines(projectId)
projectExecution.getBaseline(baselineId)
projectExecution.setActiveBaseline(baselineId)
projectExecution.deleteBaseline(baselineId)

// === التقويم ===
projectExecution.getCalendar(projectId)
projectExecution.upsertCalendar(projectId, data)

// === Checklists ===
projectExecution.listChecklists(activityId)
projectExecution.createChecklistItem(activityId, title)
projectExecution.toggleChecklistItem(checklistId)
projectExecution.deleteChecklistItem(checklistId)
projectExecution.reorderChecklist(activityId, orderedIds)

// === التحليلات ===
projectExecution.getDashboard(projectId)
  // إرجاع: إجمالي التقدم، المراحل المتعثرة، الأنشطة الحرجة، 
  //         مقارنة مخطط/فعلي، صحة الجدول
projectExecution.getCriticalPath(projectId)
projectExecution.getLookahead(projectId, weeks: 2|4|6)
projectExecution.getDelayAnalysis(projectId)
projectExecution.getPlannedVsActual(projectId, baselineId?)
```

**تحديث endpoints الحالية:**
- `projectTimeline.*` يجب أن تبقى تعمل (backward compatible)
- أو تُدمج تحت `projectExecution.*` مع إعادة توجيه

#### Phase 1C: الواجهة — الوضع الأساسي (Simple Mode)

**صفحة التنفيذ الرئيسية `/projects/[projectId]/execution`:**

```
┌─────────────────────────────────────────────────────┐
│  📊 ملخص صحة المشروع                                │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐               │
│  │ 45%  │ │ 2/5  │ │ 1    │ │ 3    │               │
│  │إجمالي│ │مكتمل │ │متعثر │ │قادم  │               │
│  └──────┘ └──────┘ └──────┘ └──────┘               │
│                                                     │
│  [بطاقات] [جدول] [جانت ←وضع متقدم]                 │
│                                                     │
│  ┌─ أعمال الهيكل ──────────────────────── 50% ─────┐│
│  │ قيد التنفيذ | على المسار                         ││
│  │ 📅 27 فبراير - 27 مارس 2026                      ││
│  │ 👷 أحمد (مهندس موقع)                              ││
│  │ ████████████░░░░░░░░░░░░ 50%                     ││
│  │                                                   ││
│  │ ⏰ تنبيه: 15 يوم متبقي                            ││
│  │                                                   ││
│  │ [📷 3 صور] [📋 تقرير اليوم] [⚠️ 1 مشكلة مفتوحة] ││
│  │                                                   ││
│  │ ▸ قائمة الأنشطة (اختياري — يتوسع عند الضغط)      ││
│  │   ☑ حفر الأساسات                                  ││
│  │   ☑ صب النظافة                                    ││
│  │   ☐ تسليح القواعد                                 ││
│  │   ☐ صب القواعد                                    ││
│  └───────────────────────────────────────────────────┘│
│                                                     │
│  ┌─ أعمال التشطيبات ──────────────────── 0% ───────┐│
│  │ مخطط | على المسار                                ││
│  │ 📅 27 فبراير - 27 أبريل 2026                      ││
│  │ ░░░░░░░░░░░░░░░░░░░░░░░ 0%                       ││
│  └───────────────────────────────────────────────────┘│
│                                                     │
│  [+ إضافة مرحلة]                                    │
│                                                     │
│  ─── 📸 صور المشروع الأخيرة ───                     │
│  ─── 📋 آخر التقارير اليومية ───                     │
└─────────────────────────────────────────────────────┘
```

**القرارات التصميمية للوضع الأساسي:**

1. **بطاقات المراحل هي الواجهة الرئيسية** — كل مرحلة بطاقة قابلة للتوسع
2. **داخل البطاقة:** صور + تقارير + مشاكل + أنشطة (كل شيء في مكان واحد)
3. **التقدم يحسب تلقائياً** من الأنشطة (إذا أضاف أنشطة) أو يدوي (إذا لم يضف)
4. **تنبيهات بصرية واضحة:** أيقونة تحذير + لون أحمر للمتعثر
5. **لا يوجد جانت في الوضع الأساسي** — فقط بطاقات + جدول بسيط
6. **زر واضح "التبديل للوضع المتقدم"** ينقل للجانت

**قوالب مراحل جاهزة (حسب نوع المشروع):**

```typescript
const VILLA_TEMPLATE = [
  { name: "أعمال الحفر والأساسات", weight: 15, activities: [
    "حفر الأساسات", "صب النظافة", "تسليح القواعد", "صب القواعد",
    "ردم وحدل", "عزل الأساسات"
  ]},
  { name: "أعمال الهيكل", weight: 25, activities: [
    "أعمدة الدور الأرضي", "سقف الدور الأرضي", 
    "أعمدة الدور الأول", "سقف الدور الأول",
    "الدرج", "خزان المياه"
  ]},
  { name: "أعمال البناء", weight: 15, activities: [
    "بناء الجدران الخارجية", "بناء الجدران الداخلية", "فتحات الأبواب والنوافذ"
  ]},
  { name: "أعمال العزل", weight: 5, activities: [
    "عزل مائي", "عزل حراري"
  ]},
  { name: "أعمال السباكة والكهرباء", weight: 15, activities: [
    "تمديدات السباكة", "تمديدات الكهرباء", "تمديدات التكييف", "اختبارات الضغط"
  ]},
  { name: "أعمال التشطيبات", weight: 20, activities: [
    "اللياسة", "البلاط والسيراميك", "الدهانات", "أعمال الجبس",
    "الأبواب والنوافذ", "أعمال الألمنيوم", "المطابخ والخزائن"
  ]},
  { name: "أعمال خارجية", weight: 5, activities: [
    "تنسيق الحدائق", "السور والبوابة", "مواقف السيارات", "التنظيف النهائي"
  ]}
];

const APARTMENT_BUILDING_TEMPLATE = [...];
const WAREHOUSE_TEMPLATE = [...];
const RENOVATION_TEMPLATE = [...];
```

### Phase 2: الوضع المتقدم (Advanced Mode)
> **الهدف:** جانت احترافي مستوحى من Primavera/MS Project لكن بواجهة مسار البسيطة

#### Phase 2A: جانت تفاعلي

**المكوّن:** `<AdvancedGanttChart />`

```
┌─────────────────────────────────┬──────────────────────────────────┐
│ اسم النشاط    │مسؤول│ % │حالة│  فبراير    │    مارس    │   أبريل  │
├─────────────────────────────────┼──────────────────────────────────┤
│ ▼ أعمال الهيكل                 │  ████████████████                │
│   ├ حفر الأساسات  أحمد 100 ✅ │  ████                            │
│   ├ صب النظافة    أحمد 100 ✅ │    ██                            │
│   ├ تسليح القواعد  خالد  60 🔵│      ████████                    │
│   │               FS ──────────┤ خط أساس ░░░░░░░░                │
│   ├ صب القواعد    خالد   0 ⬜│           ↓ ████                  │
│   └ عزل الأساسات  سعد    0 ⬜│                ███               │
│ ▼ أعمال التشطيبات              │                    ████████████ │
│   ├ اللياسة       محمد   0 ⬜│                    ████          │
│   └ البلاط       عبدالله 0 ⬜│                        ████████  │
│                                │                                  │
│  ─── اليوم                     │        ▼                         │
│  ─── مسار حرج                  │  ████████████████████████████    │
│  ─── خط أساس                   │  ░░░░░░░░░░░░░░░░░░░░░░░░░░░░   │
└─────────────────────────────────┴──────────────────────────────────┘
│ [🔍 تكبير/تصغير] [📅 أسبوع|شهر|ربع] [⚡ مسار حرج] [📊 Baseline] │
└──────────────────────────────────────────────────────────────────────┘
```

**وظائف الجانت المتقدم:**

1. **Drag & Drop:**
   - سحب أفقي لتغيير التواريخ
   - سحب حافة الشريط لتغيير المدة
   - خط ربط من نشاط لنشاط لإنشاء Dependency

2. **WBS Hierarchy:**
   - طي/فتح المراحل
   - إضافة أنشطة فرعية بالسحب
   - ترقيم تلقائي (1.1, 1.2, 2.1...)

3. **Dependencies:**
   - رسم خطوط بين الأشرطة
   - أنواع: FS, SS, FF, SF
   - Lag/Lead (بالأيام)
   - تحذير عند إنشاء حلقة (Circular)

4. **مسار حرج:**
   - حساب تلقائي باستخدام CPM (Critical Path Method)
   - تلوين الأنشطة الحرجة بالأحمر
   - عرض Total Float لكل نشاط

5. **خط أساس Baseline:**
   - زر "اعتماد الخطة" يحفظ Baseline
   - عرض شريطين: المخطط (ملون) والأساس (رمادي شفاف)
   - Variance بالأيام لكل نشاط

6. **مقياس الزمن:**
   - أسبوعي / شهري / ربع سنوي
   - تكبير/تصغير
   - خط "اليوم" عمودي

7. **تقويم مخصص:**
   - أيام العمل (السعودية: أحد-خميس أو أحد-جمعة)
   - إجازات رسمية + إجازات مخصصة
   - ساعات العمل اليومية

#### Phase 2B: Lookahead (خطة قصيرة المدى)

**صفحة مخصصة أو Tab داخل التنفيذ:**

```
┌─────────────────────────────────────────────────────────┐
│  📋 Lookahead — الأسابيع 4 القادمة                       │
│                                                         │
│  🔴 هذا الأسبوع (27 فبراير - 5 مارس)                   │
│  ├ تسليح القواعد (60% → يجب إنهاء هذا الأسبوع)         │
│  └ بدء صب القواعد (يوم الأربعاء)                        │
│                                                         │
│  🟡 الأسبوع القادم (6-12 مارس)                          │
│  ├ إكمال صب القواعد                                     │
│  └ بدء عزل الأساسات                                     │
│                                                         │
│  ⚪ الأسبوع الثالث (13-19 مارس)                          │
│  └ إكمال عزل الأساسات                                    │
│                                                         │
│  ⚪ الأسبوع الرابع (20-26 مارس)                          │
│  └ بدء أعمدة الدور الأرضي                               │
│                                                         │
│  ⚠️ تنبيهات:                                             │
│  • تسليح القواعد متأخر 3 أيام — يجب تسريع               │
│  • توريد حديد مطلوب قبل 10 مارس                         │
└─────────────────────────────────────────────────────────┘
```

#### Phase 2C: تقارير وتحليلات

**1. تقرير Planned vs Actual:**
- جدول بكل نشاط: تاريخ مخطط vs فعلي vs فرق بالأيام
- رسم بياني: S-Curve (منحنى التقدم التراكمي)

**2. تقرير أسباب التأخير:**
- قائمة الأنشطة المتأخرة
- سبب التأخير (يدوي: توريد/مقاول باطن/اعتماد/طقس/تصميم/مالك)
- تأثير على الأنشطة التابعة (عدد الأيام)

**3. لوحة تحكم التنفيذ:**
- إجمالي التقدم (%) مع S-Curve
- عدد الأنشطة حسب الحالة (pie chart)
- أنشطة المسار الحرج المتبقية
- مقارنة: تاريخ الانتهاء المخطط vs المتوقع

### Phase 3: الربط المالي والتعاقدي
> **الهدف:** ربط التنفيذ بالمستخلصات وعقود الباطن

#### Phase 3A: ربط Milestones بشروط الدفع

**المنطق:**
```
إذا milestone.progress >= milestone.paymentThreshold:
  → إنشاء إشعار "مرحلة مستحقة للمستخلص"
  → ربط بـ ContractPaymentTerm المناظر
  → تسهيل إنشاء Claim مباشرة من صفحة التنفيذ
```

**الإضافات على Schema:**
```prisma
// إضافة على ProjectMilestone
paymentTermId  String?  // ربط بشرط الدفع
paymentThreshold Decimal? @default(100) // نسبة الإنجاز المطلوبة للاستحقاق
```

#### Phase 3B: طبقة عقود الباطن

**في الجانت المتقدم:**
- كل مقاول باطن يظهر بلون مختلف
- أنشطة المقاول تظهر في نفس الجانت أو كطبقة منفصلة
- ربط SubcontractPaymentTerm بالأنشطة المنجزة

#### Phase 3C: أوامر التغيير والتأثير الزمني

**عند اعتماد أمر تغيير:**
1. حفظ Baseline جديد تلقائي (Baseline v2, v3...)
2. إضافة أنشطة جديدة من أمر التغيير
3. حساب التأثير على تاريخ الانتهاء
4. توثيق الفرق بين Baseline القديم والجديد

### Phase 4: التكامل والاستيراد/تصدير
> **مؤجلة — تُنفذ بعد استقرار Phase 1-3**

- Import/Export Excel/CSV للأنشطة
- تصدير PDF للجانت
- مستقبلاً: MS Project XML / Primavera P6 XER

---

## القواعد المعمارية (يجب اتباعها)

### 1. الـ API

```typescript
// كل endpoint يتبع هذا النمط:
export const executionRouter = router({
  listActivities: protectedProcedure
    .input(z.object({ 
      projectId: z.string(),
      milestoneId: z.string().optional(),
    }))
    .query(async ({ input, ctx }) => {
      // 1. التحقق من العضوية
      const membership = await verifyProjectAccess(ctx, input.projectId);
      // 2. التحقق من الصلاحية
      checkPermission(membership, 'projects.view');
      // 3. جلب البيانات مع organizationId filter
      return db.projectActivity.findMany({
        where: { 
          projectId: input.projectId,
          project: { organizationId: ctx.session.organizationId }
        }
      });
    }),
});
```

### 2. الواجهة

```typescript
// مسار الملفات:
// apps/web/modules/saas/projects/components/execution/
//   ├── SimpleMode/
//   │   ├── MilestoneCard.tsx
//   │   ├── MilestoneList.tsx
//   │   ├── ActivityChecklist.tsx
//   │   ├── QuickProgressUpdate.tsx
//   │   └── ExecutionDashboard.tsx
//   ├── AdvancedMode/
//   │   ├── GanttChart/
//   │   │   ├── GanttChart.tsx
//   │   │   ├── GanttRow.tsx
//   │   │   ├── GanttBar.tsx
//   │   │   ├── DependencyLine.tsx
//   │   │   ├── TimeScale.tsx
//   │   │   ├── CriticalPath.tsx
//   │   │   └── BaselineOverlay.tsx
//   │   ├── Lookahead.tsx
//   │   ├── PlannedVsActual.tsx
//   │   └── DelayAnalysis.tsx
//   └── Shared/
//       ├── MilestoneForm.tsx
//       ├── ActivityForm.tsx
//       ├── DependencyDialog.tsx
//       ├── BaselineDialog.tsx
//       ├── CalendarSettings.tsx
//       └── ExecutionModeToggle.tsx

// الصفحات:
// apps/web/app/(saas)/app/(organizations)/[organizationSlug]/projects/[projectId]/execution/
//   ├── page.tsx              (الصفحة الرئيسية — Simple Mode بالافتراضي)
//   ├── advanced/page.tsx     (الوضع المتقدم — الجانت)
//   ├── lookahead/page.tsx    (Lookahead)
//   ├── analysis/page.tsx     (التحليلات والتقارير)
//   ├── new-report/page.tsx   (موجود — تقرير يومي)
//   ├── new-issue/page.tsx    (موجود — ملاحظة)
//   └── upload/page.tsx       (موجود — رفع صور)
```

### 3. الترجمة (i18n)

```json
// كل نص جديد يُضاف في:
// packages/i18n/translations/ar.json + en.json
{
  "execution": {
    "title": "التنفيذ",
    "simpleMode": "الوضع الأساسي",
    "advancedMode": "الوضع المتقدم",
    "milestone": "مرحلة",
    "activity": "نشاط",
    "dependency": "علاقة",
    "baseline": "خط أساس",
    "criticalPath": "المسار الحرج",
    "lookahead": "خطة قصيرة المدى",
    "plannedVsActual": "المخطط مقابل الفعلي",
    "delayAnalysis": "تحليل التأخيرات",
    "onTrack": "على المسار",
    "delayed": "متعثر",
    "ahead": "متقدم",
    "daysRemaining": "{{count}} يوم متبقي",
    "daysDelayed": "متأخر {{count}} يوم",
    "progressUpdate": "تحديث التقدم",
    "addActivity": "إضافة نشاط",
    "addDependency": "إضافة علاقة",
    "createBaseline": "اعتماد الخطة",
    "templates": {
      "villa": "فيلا سكنية",
      "apartment": "عمارة سكنية",
      "warehouse": "مستودع",
      "renovation": "تجديد وتشطيب"
    },
    "delayReasons": {
      "supply": "تأخر توريد",
      "subcontractor": "مقاول باطن",
      "approval": "انتظار اعتماد",
      "weather": "ظروف جوية",
      "design": "تعديل تصميمي",
      "owner": "طلب المالك",
      "financial": "مالي",
      "other": "أخرى"
    }
  }
}
```

### 4. الصلاحيات

```typescript
// إضافة على مصفوفة الصلاحيات الحالية:
// لا نحتاج صلاحيات جديدة — نستخدم الموجود:
// projects.view → قراءة التنفيذ
// projects.edit → تعديل التنفيذ (إضافة/تحديث أنشطة ومراحل)
```

---

## خوارزميات مطلوبة

### 1. حساب المسار الحرج (CPM)

```
Input: قائمة الأنشطة + العلاقات + المدد
Output: ES, EF, LS, LF, Total Float لكل نشاط

Forward Pass (من البداية للنهاية):
  ES(i) = max(EF(predecessors) + lag)
  EF(i) = ES(i) + duration(i)

Backward Pass (من النهاية للبداية):
  LF(i) = min(LS(successors) - lag)
  LS(i) = LF(i) - duration(i)

Total Float = LS(i) - ES(i)
Critical Path = الأنشطة التي Total Float = 0
```

### 2. Auto-scheduling (إعادة جدولة تلقائية)

```
عند تغيير تاريخ أو مدة نشاط:
1. إعادة حساب Forward Pass للأنشطة المتأثرة
2. تحريك الأنشطة التابعة (Successors) تلقائياً
3. التحقق من عدم كسر Constraints
4. تحديث المسار الحرج
5. تحديث نسب التقدم للمراحل الأب
```

### 3. حساب تقدم المرحلة من الأنشطة

```
milestone.progress = Σ(activity.progress × activity.weight) / Σ(activity.weight)

إذا لم تُعيّن أوزان:
  milestone.progress = Σ(activity.progress) / count(activities)
```

### 4. كشف الحلقات (Cycle Detection)

```
استخدام DFS (Depth-First Search):
عند إضافة dependency جديد:
  1. ابنِ graph من العلاقات
  2. شغّل DFS من successor
  3. إذا وصلنا لـ predecessor = حلقة → رفض العلاقة
```

---

## ملاحظات مهمة

### التوافق مع البيانات الحالية
- البيانات الحالية في `ProjectMilestone` يجب أن تبقى تعمل
- المراحل الحالية تظهر في الوضع الأساسي بدون تغيير
- الأنشطة اختيارية — المرحلة تعمل بدون أنشطة

### الأداء
- الجانت يجب أن يعمل بسلاسة مع 200+ نشاط
- استخدام virtualization للقوائم الطويلة
- حساب CPM يتم server-side وليس client-side
- Cache نتائج CPM وأعد حسابها فقط عند التعديل

### الموبايل
- الوضع الأساسي يعمل 100% على الموبايل
- الجانت المتقدم يعرض "اقتراح استخدام الكمبيوتر" على شاشات < 768px
- أو يعرض نسخة مبسطة: قائمة الأنشطة مع تواريخ بدل الأشرطة

### مكتبات مقترحة (لا تثبّت بدون تأكيد)
- **@dnd-kit** — للسحب والإفلات (متوافق مع React 19)
- **date-fns** — حسابات التواريخ وأيام العمل
- لا نستخدم مكتبة جانت خارجية — نبنيها custom بـ SVG/Canvas

---

## الإخراج المطلوب (PLAN MODE)

قدّم خطة تفصيلية تشمل:

1. **قائمة كل الملفات** التي ستُنشأ أو تُعدّل مع وصف التغيير
2. **ترتيب التنفيذ** (أيهم أولاً)
3. **Schema migration** المطلوبة بالتفصيل
4. **تقدير الجهد** لكل مرحلة فرعية
5. **المخاطر والتحديات** المتوقعة
6. **قرارات تقنية** تحتاج إجابة قبل البدء

**لا تكتب كود. فقط خطة.**
